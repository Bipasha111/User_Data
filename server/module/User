
const mongoose = require('mongoose')

const userSchema = mongoose.Schema({
    email:{
        type:String,
        // required:true,
        // unique:true,
        // lowercase:true,

    },
    password:{
        type:String
    }
})
module.exports = mongoose.model("user_login_table",userSchema);